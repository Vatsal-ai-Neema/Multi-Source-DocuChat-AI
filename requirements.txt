# DocuChat AI v2 — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core Framework ────────────────────────────────────────────
streamlit>=1.32.0

# ── LangChain (RAG Pipeline) ──────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=0.0.3
langchain-text-splitters>=0.2.0

# ── Vector Database ───────────────────────────────────────────
faiss-cpu>=1.7.4

# ── Embeddings (Free, Local) ──────────────────────────────────
sentence-transformers>=2.7.0    # Used for embeddings + [07] NLI model
torch>=2.0.0

# ── LLM (Google Gemini - Free Tier) ──────────────────────────
google-genai>=1.0.0
openai>=1.0.0

# ── PDF Processing ────────────────────────────────────────────
pdfplumber>=0.11.0              # PDF text + [06] table extraction
PyPDF2>=3.0.0                   # Fallback PDF reader
PyMuPDF>=1.24.0                 # Render PDF pages to images for vision fallback

# ── [06] Visual Intelligence ──────────────────────────────────
pandas>=2.0.0                   # Table DataFrame handling
tabulate>=0.9.0                 # DataFrame.to_markdown() support

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0
numpy>=1.24.0
pytest>=8.0.0

# ── Notes on Upgrades ─────────────────────────────────────────
# [04] HyDE         — uses google-genai (already above), no extra lib
# [05] Socratic     — uses google-genai (already above), no extra lib
# [06] Visual Intel — uses pdfplumber + pandas + google-genai
# [07] Contradiction — uses sentence-transformers (cross-encoder/nli-deberta-v3-small)
#                      Model downloads automatically on first use (~85MB, local, free)
